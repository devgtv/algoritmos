#include<iostream>
using namespace std;

int main () {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int m,n;
    // (m * n) / 2.
    cin >> m >> n;


    int maximo = (m * n) / 2;
    cout << maximo << endl;

    return 0;
}