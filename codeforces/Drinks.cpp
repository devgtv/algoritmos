#include <bits/stdc++.h>
#define int long long
#define endl '\n'
#define pi 3.14159

using namespace std;

signed main () {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> bomba(n);

    for(int i = 0 ; i < n;i++) {
        cin >> bomba[i];
    }




    for(int i = 0 ; i < n;i++) {
        
    }
    return 0;
}